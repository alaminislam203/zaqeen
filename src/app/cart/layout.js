export const metadata = {
  title: 'Shopping Cart - Zaqeen',
  description: 'Review the items in your shopping cart and proceed to checkout.',
};

export default function CartLayout({ children }) {
  return <>{children}</>;
}
