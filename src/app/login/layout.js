export const metadata = {
  title: 'Login - Zaqeen',
  description: 'Login to your Zaqeen account to access your orders and wishlist.',
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
