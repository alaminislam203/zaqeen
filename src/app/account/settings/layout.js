export const metadata = {
  title: 'Account Settings - Zaqeen',
  description: 'Manage your account details, password, and preferences.',
};

export default function AccountSettingsLayout({ children }) {
  return <>{children}</>;
}
