export const metadata = {
  title: 'My Wishlist - Zaqeen',
  description: 'Manage your wishlist and save your favorite items for later.',
};

export default function WishlistLayout({ children }) {
  return <>{children}</>;
}
