import Topbar from '@/components/TopBar';
import Navbar from '@/components/Navbar';

const Header = () => {
  return (
    <header>
      <Topbar />
      <Navbar />
    </header>
  );
};

export default Header;